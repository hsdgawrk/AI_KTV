# Explicit Replace for Song Import

**Song Import** will reject an import whose generated **Song** id already exists unless the maintainer explicitly requests **Replace Imported Song**. This avoids accidental asset replacement in the **Song Asset Directory** and keeps repeated imports aligned with the existing rule that **Refresh Song Library** does not rewrite already-created **Queued Song** or **Current Song** snapshots.
