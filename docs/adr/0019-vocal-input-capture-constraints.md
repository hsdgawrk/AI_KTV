# Vocal Input Capture Constraints

The first **Vocal Input** capture will request browser echo cancellation and noise suppression, while leaving automatic gain control disabled. Local room singing needs some protection against speaker bleed from the **Master**, but automatic gain control can make singing dynamics unstable; richer vocal effects such as reverb, compression, or equalisation remain outside the first implementation.
