# User Todo

- Put real songs in the configured **Song Asset Directory**. Each song directory should include a `song.json` manifest and browser-playable **Original Vocal** and **Accompaniment** files.
- Add JSON **Timed Lyrics** files referenced from each `song.json`, so **Progressive Lyric Highlight** stays aligned with the playable assets.
- Add **MV** assets later by referencing them from `song.json`. **MV** assets remain lower priority and are not required for the first playback slice.
